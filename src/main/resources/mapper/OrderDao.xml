<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jinunn.dao.OrederDao">

    <!--创建订单-->
    <insert id="createOrder" parameterType="com.jinunn.domian.Oreder" useGeneratedKeys="true" keyProperty="id">
        insert  into  stock_order values(#{id},#{sid},#{name},#{createTime})
    </insert>
</mapper>